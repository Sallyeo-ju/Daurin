{
  "name": "daurin-monorepo-root",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "postinstall": "npm --prefix backend ci",
    "build": "npm --prefix backend run build",
    "start": "npm --prefix backend run start:prod",
    "dev": "npm --prefix backend run start:dev"
  }
}
